# Recent NRT only. Existing retrospective rebuild submissions do not read this file.
# Required for operational NRT: disabling blocks scheduling, not a legacy-mode switch.
enabled = true
# Validated by paired benchmark 4555698; "reference" restores previous writers.
reconciliation_writer_profile = "validated_chunks_reuse_v1"
# Validated by paired NLDAS-2/HRRR benchmark 4556629; rollback: "reference".
baseline_writer_profile = "validated_source_chunks_v1"
lookback_days = 7
maximum_old_replacement_days = 2
assembly_workers = 4
maximum_native_donor_km = 40
activation_receipt = "forcing/status/nrt-gfs/activation.json"
envelope = "forcing/static/coverage/conus/nldas2_seven_met_static_envelope_v4.nc"
geometry = "forcing/static/remapping/nwm_conus_1km/gfs_hrrr_gap_weights_v1.npz"
conservative_weights = "forcing/static/remapping/nwm_conus_1km/gfs_gap_conservative_v1.nc"
cache = "forcing/inputs/noaa/gfs/surface_025"
model_terrain = "nwm/static/operational/nwm.v3.1.6/domain/wrfinput_CONUS_NLDAS2.nc"
